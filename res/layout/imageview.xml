<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="fill_parent"
  android:layout_height="fill_parent">
  
  <ImageView android:id="@+id/imageView"
    android:scaleType="centerInside"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" />
    
	<LinearLayout android:layout_width="fill_parent" android:layout_height="fill_parent"
		android:orientation="horizontal"
		android:gravity="bottom|center">
		
		<Button android:id="@+id/deleteImageButton" android:text="@string/deleteImageButtonLabel" android:onClick="deleteImage"
			android:layout_width="wrap_content" android:layout_height="wrap_content" />

		<Button android:id="@+id/shareImageButton" android:text="@string/shareImageButtonLabel" android:onClick="shareImage"
			android:layout_width="wrap_content" android:layout_height="wrap_content" />

		<Button android:id="@+id/shareHtmlButton" android:text="@string/shareHtmlButtonLabel" android:onClick="shareHtml"
			android:layout_width="wrap_content" android:layout_height="wrap_content" />

		<Button android:id="@+id/exitViewImageButton" android:text="@string/exitViewImageButtonLabel" android:onClick="goBack"
			android:layout_width="wrap_content" android:layout_height="wrap_content" />

	</LinearLayout>
  
  
</FrameLayout>
